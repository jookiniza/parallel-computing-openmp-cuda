# parallel-computing-openmp-cuda
Benchmark-driven parallel computing project in C, OpenMP and CUDA, comparing serial, CPU-parallel and GPU-parallel implementations for numerical integration and matrix-vector multiplication.
